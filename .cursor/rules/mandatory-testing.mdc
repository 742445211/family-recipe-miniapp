---
description: 每次代码改动后必须验证，未完成测试不得声称完成
alwaysApply: true
---

# 改动后必须测试

完成任何代码改动后，**必须先验证再回复「已完成」**。禁止只改代码不跑检查。

## 最低验收（每次改动都要做）

1. **语法/加载**：对改动的 `.js` 文件做语法检查或模块加载模拟，确认无解析错误。
2. **编译**：在微信开发者工具中确认编译通过、控制台无红字报错（白屏通常来自启动期 JS 异常）。
3. **行为**：手动走一遍受影响的页面路径（Tab 切换、跳转、按钮），确认 UI 正常、无白屏。

## 按改动类型追加

| 改动类型 | 额外验证 |
|---------|---------|
| `utils/api.js` 或新接口 | `curl` 生产/本地 `BASE_URL` 对应路径，确认 `code === 0` 或预期错误码 |
| `app.js` 启动逻辑 | 冷启动小程序，确认首页可渲染；避免在 `app.js` 顶层 `require('./utils/api')` 引发循环依赖 |
| 页面 `onLoad`/`onShow` | 进入该页 + 返回 + 再进入；异步逻辑用 `.then()` 或确保不阻塞首屏 |
| WXML 条件渲染 | 切换 `wx:if` 相关 data，确认显示/隐藏符合预期 |

## 完成标准

- 有**可引用的验证结果**（命令输出、curl 响应、或开发者工具无报错）再标记任务完成。
- 若无法在本地跑微信开发者工具，说明阻塞原因，并至少完成语法检查 + API curl（如适用）；**不要**在无证据时断言「已修复」。

## 反例

```
❌ 改完 app.js / mine.js 后直接说「已实现」
✅ 改完后 curl /app/features → 模拟加载 app.js → 说明需在开发者工具确认 Tab 页无白屏
```
